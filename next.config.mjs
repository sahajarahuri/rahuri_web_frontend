/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["img1.wsimg.com", "i.ytimg.com"], // Add the domains you'll be using for images
  },
};

export default nextConfig;
